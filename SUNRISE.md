# サンライズ空席通知

対象：2026年9月24日 岡山22:34発 → 9月25日 東京07:08着、大人1名。

既存のピックルボール用外部スケジューラーから15分おきに起動する。
クラウドで動くためMacを開いておく必要はない。実行待ち・サイト応答により
通知が遅れる場合があり、瞬時の検知を保証するものではない。

瀬戸・出雲のノビノビ座席、A寝台、B寝台を検索する。
未ログインの検索結果はB寝台を種類別に表示しないため、シングルツイン、
シングル、ソロ、サンライズツインのどれが空いているかは通知で断定しない。
禁煙・喫煙は表示どおりに区別する。料金は検索画面で未表示のため推定しない。

新しく空いた区分のみ既存Slack #reservationへ投稿する。
満席・同じ空席が続いている場合は通知しない。一度満席を確認して再度空いたら
再通知する。初回に空席があれば通知する。予約・購入はしない。
通知には日時、列車、区分、表示された空席情報、公式予約リンクを含む。

照会失敗は満席扱いにせずジョブを失敗にし、前回の状態を保持する。
Slack送信が失敗した場合も状態を進めず、次回に再送する。
出発時刻以降は検索・通知を停止する。夜間23:30～翌5:00は照会しない。

手動確認：GitHub Actionsの `sunrise-availability-monitor` を実行。
`notify=false`（初期値）は実際の空席を読むだけで通知・状態更新をしない。
停止：`.github/workflows/pickleball.yml` の `sunrise` ジョブを削除する。

状態：`sunrise_state.json`。機密情報は既存のGitHub Secretsを使用する。
